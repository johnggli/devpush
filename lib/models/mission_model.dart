class Mission {
  int id;
  String title;
  String description;
  int level;
  int current;
  int goal;
  bool completed;

  Mission(this.id, this.title, this.description, this.level, this.current,
      this.goal, this.completed);
}
